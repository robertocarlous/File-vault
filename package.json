{
  "name": "hardhat-project",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run deploy/deploy.js --network calibrationnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.19",
    "hardhat-deploy": "^0.12.4",
    "hardhat-deploy-ethers": "^0.4.2"
  }
}
